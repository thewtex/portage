# /etc/conf.d/udev: config file for udev

# We discourage users to disable persistent-net, since it may
# lead to unpredictable network interface naming.

persistent_net_disable="no"

# Device tarball disabled by default

device_tarball="no"

